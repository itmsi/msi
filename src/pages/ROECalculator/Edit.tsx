import CreateROECalculator from './Create';

export default function EditROECalculator() {
  // The Create component already handles edit mode when calculatorId param is present
  return <CreateROECalculator />;
}